<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- The Tower radar, still: one ring, one center. Same geometry as the mark
       in src/Glyph.swift, with an explicit color (an icon theme renders this
       outside any page, so currentColor has nothing to inherit from). -->
  <circle cx="50" cy="50" r="33" fill="none" stroke="#0c0c0e" stroke-width="12"/>
  <circle cx="50" cy="50" r="10" fill="#0c0c0e"/>
</svg>
