# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Built product — regenerate with ./build.sh
Tower.app/

# Local runtime state (created at ~/.tower, never in-repo, but just in case)
.tower/
Tower.app.zip

# Debian package build — regenerate with packaging/deb/build.sh
build/
dist/
